# NetworkPolicy intentionally omitted for longhorn-system
#
# Longhorn components require complex intra-namespace and API server webhook
# communication that is difficult to fully enumerate with NetworkPolicy.
# Longhorn pods are privileged (SYS_ADMIN) with no external attack surface.
# Security posture is maintained via encryption at rest instead.
# See ADR-XXX for full rationale.