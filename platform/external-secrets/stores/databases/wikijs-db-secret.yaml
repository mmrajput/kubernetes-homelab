apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wikijs-initdb-secret
  namespace: databases
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: wikijs-initdb-secret
    creationPolicy: Owner
  data:
    # CNPG initdb requires keys named exactly 'username' and 'password' — not configurable
    - secretKey: username
      remoteRef:
        key: secret/data/databases/wikijs
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/databases/wikijs
        property: password